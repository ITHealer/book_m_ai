# Set this to your domain name (default: http://localhost:5173)
# Example: PUBLIC_ORIGIN=healer.mydomain.com
PUBLIC_ORIGIN=http://localhost:5173
# Set this to true if you're using HTTPS (default: false)
# Example: PUBLIC_HTTPS_ONLY=true
PUBLIC_HTTPS_ONLY=false
# Change to the port you want the app to listen on (default: 5173)
# Example: PORT=80
PORT=5173
# Set this to true if you want to disable public signups (default: false)
PUBLIC_SIGNUP_DISABLED=false

# AI Service Configuration
# Base URL for the AI service API (default: http://localhost:8000)
# Example: HEALER_AI_BASE_URL=https://ai.healer.app
HEALER_AI_BASE_URL=http://localhost:8000
# API key for authenticating with the AI service
# Example: HEALER_AI_API_KEY=your-secret-key-here
HEALER_AI_API_KEY=

# Background Workers Configuration
# Enable background workers for AI embeddings and health checks (default: false in dev, true in production)
# Example: ENABLE_WORKERS=true
ENABLE_WORKERS=true
# Health check interval in minutes (default: 60)
# Example: HEALTH_CHECK_INTERVAL_MINUTES=30
HEALTH_CHECK_INTERVAL_MINUTES=60
# Embedding generation interval in minutes (default: 30)
# Example: EMBEDDING_INTERVAL_MINUTES=15
EMBEDDING_INTERVAL_MINUTES=30